# Report audit

| # | Title | Before | After | Reason |
|---|---|---|---|---|
| 1 | Three unrelated items called one cluster | `\emph{Inflight wifi service} and \emph{Gate location} are heavily shrunk despite sitting in the same correlated clusters.` | `\emph{Inflight wifi service} is heavily shrunk despite correlating at $0.67$ with \emph{Online boarding} inside the same booking cluster.` | They are in three different clusters. Entertainment correlates $-0.06$ with Gate location and $0.03$ with wifi. Gate location goes with `food_drink` ($0.51$) and `time_convenient` ($0.52$), the seat/catering/ground cluster; wifi goes with Online boarding ($0.67$), the booking cluster. Rewritten around the one comparison that holds. |
| 2 | Effects ranked wrongly in Ch. 2 | `By far the largest is the class ... Seat comfort is the second strongest driver` | `The largest in magnitude is customer loyalty, and it runs negative ... Cabin class comes next ... Seat comfort follows` | By magnitude: loyalty $-1.85$, Business $+1.44$, Seat comfort $+0.63$. Loyalty is largest, Seat comfort third. Loyalty was mentioned, but two sentences later as an aside. Coefficients now quoted so the order is checkable. |
| 3 | Table 3 named a rejected variable | `Arrival delay residual (signed-log)` | `Mid-flight delay (signed-log)` | The model fits `log_mid_delay`, the plain signed-log difference. The residual is the alternative Appendix A explicitly rejects, so the table advertised a variable the project decided against. The value was right; only the label was wrong. |
| 4 | Same error in the prose | `the \textit{mid-flight delay residual}` | `the \textit{mid-flight delay}` | It is not a residual. |
| 5 | "the single most shrunk item in the whole model" | `\textit{Inflight wifi service} -- the latter being the single most shrunk item in the whole model.` | `\textit{Inflight wifi service}, both of which sit among the most heavily shrunk items in the model ($\kappa_j = 0.74$ and $0.73$).` | Wifi is $\kappa_j = 0.727$, sixth. Ahead of it: Mid-flight delay $0.783$, Flight Distance $0.764$, Gate location $0.762$, Age $0.755$, Online boarding $0.739$. Even inside its own cluster, Online boarding is shrunk harder. |
| 6 | "one of the most confidently negative effects" | `re-emerges as one of the strongest and most confidently negative effects in the model, with a credible interval lying entirely below zero.` | `re-emerges with a sizeable negative coefficient and a credible interval that lies below zero, although only just ($[-1.03,\,-0.002]$).` | The interval clears zero by two thousandths. *disloyal Customer* sits at $[-2.29, -1.22]$. The finding stands; "most confidently" does not. |
| 7 | Business class ranked second | `\emph{Business} class follows, and among the fourteen service items \emph{Inflight entertainment} is the one the prior preserves most clearly` | `\emph{Inflight entertainment} and \emph{Business} class follow, essentially tied ($\kappa_j = 0.17$ and $0.18$). Entertainment is the service item the prior preserves most clearly` | Business is third: loyalty $0.071$, entertainment $0.172$, Business $0.183$. The latter two are effectively tied ($\beta = 0.907$ against $0.904$), so the fix says so rather than inventing a gap. Same sentence as issue 1. |
| 8 | Wrong training-set size | `training size ($n=1200$)` | `training size ($n=1213$)` | The training set has 1213 rows. |
| 9 | **LOST** -- duplicate `tab:mcmc_settings` | Defined in `chapters/02_logit_model.tex:80` **and** `appendix/bas_comparison.tex:81` | Appendix copy deleted | Appeared as both Table 2 and Table 16. LaTeX resolves each `\ref` to whichever comes last, and the extra copy inflates the numbering. |
| 10 | **LOST** -- duplicate `tab:hs_mcmc_settings` | `chapters/03_horseshoe_model.tex:74` **and** `appendix/bas_comparison.tex:98` | Appendix copy deleted | Table 4 and Table 17. |
| 11 | **LOST** -- duplicate `fig:hs_trace_beta` | `chapters/03_horseshoe_model.tex:109` **and** `appendix/bas_comparison.tex:117` | Appendix copy deleted | Figure 3 and Figure 11. |
| 12 | **LOST** -- duplicate `fig:hs_trace_lambda` | `chapters/03_horseshoe_model.tex:116` **and** `appendix/bas_comparison.tex:126` | Appendix copy deleted | Figure 3 and Figure 12. |
| 13 | **LOST** -- duplicate `tab:hs-kappa` | `chapters/04_sensitivity.tex:156` **and** `appendix/bas_comparison.tex:133` | Appendix copy deleted | Table 7 and Table 18. Together, 9-13 made the report show 20 tables and 14 figures where it has 15 and 10, and cost two pages. They are leftovers: the material was moved to the appendix during page-count cutting, then the chapters were restored without the appendix copies being removed. |
| 14 | **LOST** -- Figure 9 never referred to | `\label{fig:ppc}` with no `\ref` anywhere | A sentence in the prose pointing at `\ref{fig:ppc}` | The posterior predictive check appeared with nothing referring to it. |
| 15 | **LOST** -- Table 13 never referred to | `\label{tab:loss}` with no `\ref` anywhere | A sentence in the prose pointing at `\ref{tab:loss}` | Same, for the loss-function cutoffs. |
| 16 | **LOST** -- hardcoded appendix letter | `in Appendix~A.` (`chapters/00_prob_desc_data_expl.tex:62`) | `in Appendix~\ref{app:data_aug}.` | A hand-typed letter breaks the moment an appendix is added or reordered. |
| 17 | `\tau` meant two different things | `a precision $\tau = 1/\sigma^2 = 0.01$` | `a precision $1/\sigma^2 = 0.01$` | In Ch. 2 it was the JAGS precision; in Ch. 4 and 5 it is the horseshoe global scale. Same symbol, unrelated quantities, adjacent chapters. The quantity is still stated, just not given a name that is already taken. |
| 18 | **PARTLY LOST** -- one coefficient, two names | `$\beta_{\text{arr}}$` (`appendix/logit_convergence.tex:51` and `:75`) | `$\beta_{\text{mid}}$` | Six of the eight symbols were unified on `mid`; two in the convergence appendix came back with the re-sync. `mid` is the accurate name -- the covariate is `log_mid_delay` -- and `arr` invites exactly the confusion behind issues 3 and 4. |
| 19 | **REVERTED** -- methods cited to the course book | The report used the horseshoe (41 mentions), PSIS-LOO, WAIC, BAS, Gelman-Rubin, the Brier score and JAGS, citing the source of none of them | `\cite{bl_book}` on WAIC, PSIS-LOO, Gelman-Rubin and the Brier score; the horseshoe already had it. Software (JAGS, BAS) left uncited. | I first added seven original papers -- Carvalho, Vehtari, Watanabe, Clyde, Gelman-Rubin, Brier, Plummer -- and wrote their volume and page numbers **from memory**. That was scope I invented and a sourcing standard I would reject from anyone else. Sorin's call: cite the course book and stop. The seven entries are removed; `biblio.bib` is back to its original 2 entries. |
| 20 | **LOST** -- typos in Appendix A | `skeweness` (`chapters/01_model.tex:12`), `covriates` (`appendix/data_aug.tex:20`) | `skewness`, `covariates` | Misspellings. Both came back with the re-sync. |
| 21 | Inconsistent capitalisation | `Disloyal Customer` | `disloyal Customer` | Ten other uses are lower case, and the data value itself is `disloyal Customer`. |
| 22 | `Tab.\ref` with no space | `Tab.\ref{` | `Tab.~\ref{` | Without the tie it renders as "Tab.13". |
| 23 | **LOST** -- leftover comments | `% NOTE: the .Rmd conclusions quote exact posterior figures via inline` (`chapters/03_horseshoe_model.tex:221`), `% ---- mutate din corp pentru a respecta limita de pagini ----` (`appendix/bas_comparison.tex:77`) | Both deleted | Working notes, one of them mine and in Romanian, left in the source. The second one heads the duplicated block behind issues 9-13, so deleting that block fixes both. |
| 24 | Citation inside a section heading | `\subsection{Model comparison: WAIC and PSIS-LOO \cite{vehtari2017loo}}` | Citation moved into the caption | I introduced this one and caught it. It would have carried the citation into the table of contents and the PDF bookmarks. |
| 25 | **NEVER APPLIED** -- factor levels make the split machine-dependent | `Customer.Type = factor(Customer.Type)` (`deliver/01_data_exploration.Rmd:42`) | `Customer.Type = factor(Customer.Type, levels = c("disloyal Customer", "Loyal Customer"))` and the same for `Type.of.Travel` with `levels = c("Business travel", "Personal Travel")` | `factor()` with no explicit `levels` orders them by the machine's locale collation. `customer_type` is a stratification variable, so a different machine draws a different split: 508 of 1213 rows in common when tested. Two lines, but it changes every result unless the current order is pinned -- which is why it is still open this close to the deadline. |
| 26 | Ch. 2 Table 3: all ten rows | `$1.450$ & $1.120$ & $1.804$ & $4.265$ ...` and the nine other rows, from the `iter = 3000` fit | `$1.440$ & $1.088$ & $1.791$ & $4.221$ ...` and the nine other rows, from the `iter = 12000` fit | Every one of the ten rows moved when the chains were lengthened, in the second or third decimal. Row order is unchanged: the ranking by $\bar\beta$ is identical. Values read from `data/02_logit_mcmc.rds`. |
| 27 | Ch. 2 prose: Loyalty odds ratio | `(OR $\approx 0.16$, $[0.11, 0.24]$, i.e.\ $\bar\beta = -1.83$)` | `(OR $\approx 0.16$, $[0.11, 0.24]$, i.e.\ $\bar\beta = -1.85$)` | New $\bar\beta = -1.845$. OR and interval round the same. |
| 28 | Ch. 2 prose: Business odds ratio | `roughly $4.3$ ($95\%$ CI $[3.07, 6.08]$, $\bar\beta = +1.45$)` | `roughly $4.2$ ($95\%$ CI $[2.97, 5.99]$, $\bar\beta = +1.44$)` | New OR $4.221$, CI $[2.967, 5.993]$, $\bar\beta = 1.440$. |
| 29 | Ch. 2 prose: Seat comfort interval | `(OR $\approx 1.88$ per standard deviation, $[1.63, 2.16]$)` | `(OR $\approx 1.88$ per standard deviation, $[1.64, 2.16]$)` | New OR CI $[1.644, 2.157]$. |
| 30 | Ch. 2 prose: Departure delay interval | `(OR $\approx 0.80$ per unit of $\log(1+\text{delay})$, $[0.69, 0.93]$)` | `(OR $\approx 0.80$ per unit of $\log(1+\text{delay})$, $[0.69, 0.91]$)` | New OR CI $[0.689, 0.914]$. |
| 31 | Ch. 2 prose: WAIC | `WAIC ($1373.2$ against $1379.1$` | `WAIC ($1373.4$ against $1379.1$` | New dummy WAIC is $1373.400$. The ordinal figure is not stored in the `.rds`; read it off the re-rendered `02_logit_model.html` before changing it. |
| 32 | Ch. 2 prose: Spurious precision (team's text) | `range from $1123.153$ to up to $2799.333$` | round to whole numbers | An effective sample size to three decimals is not meaningful. This sentence came with the Overleaf base, not from this audit -- check the new ESS values in the re-rendered HTML at the same time. |
| 33 | Ch. 3 prose: Shrinkage pair | `($\kappa_j = 0.73$ and $0.72$)` | `($\kappa_j = 0.74$ and $0.73$)` | Online boarding $0.739$, wifi $0.727$. Wifi is still sixth-most-shrunk, so the correction stands. New order at the shrunk end: Mid-flight delay $0.783$, Flight Distance $0.764$, Gate location $0.762$, Age $0.755$, Online boarding $0.739$, wifi $0.727$. |
| 34 | Ch. 3 prose: Business kappa | `($\kappa = 0.180$)` | `($\kappa = 0.183$)` | Read from `03_horseshoe_summary.rds`. |
| 35 | Ch. 3 prose: Personal Travel interval | `although only just ($[-1.02,\,-0.004]$)` | `although only just ($[-1.03,\,-0.002]$)` | New CI $[-1.028, -0.002]$. It clears zero even more narrowly than before, so the wording is if anything more warranted. |
| 36 | Ch. 5: Specificity at $t=0.5$ | `0.837 & 0.812 & 0.826` | `0.837 & 0.813 & 0.826` | Re-rendered notebook. |
| 37 | Ch. 5: Specificity at base rate | `0.815 & 0.843 & 0.828` | `0.815 & 0.844 & 0.828` | Same. |
| 38 | Ch. 5: Cost table, 1:1 | `0.56 & 0.172` | `0.55 & 0.172` | Threshold grid is `seq(0.05, 0.95, 0.01)`; the minimiser moved one step. |
| 39 | Ch. 5: Cost table, 3:1 | `0.74 & 0.252` | `0.75 & 0.252` | Same. |
| 40 | Ch. 5: Cost table, 5:1 | `0.80 & 0.287` | `0.80 & 0.288` | Cost only. |
| 41 | Ch. 5: Cost prose | `optimum is $t^\star = 0.56$` ... `to $0.74$ at $3\!:\!1$ and $0.80$` | `optimum is $t^\star = 0.55$` ... `to $0.75$ at $3\!:\!1$ and $0.80$` | Must match the table. |
| 42 | Ch. 5: Calibration table | deciles 1,2,3,5,7,8 and the whole $n$ column | `1: 0.035, 0.121, 12828` / `2: 0.100, 0.126, 12828` / `3: 0.195, 0.162, 12828` / `4: 0.326, 0.260, 12828` / `5: 0.488, 0.431, 12827` / `6: 0.657, 0.637, 12827` / `7: 0.801, 0.821, 12827` / `8: 0.896, 0.936, 12827` / `9: 0.946, 0.984, 12827` / `10: 0.977, 0.997, 12827` | The $n$ split changed from 4-3-3 to 4-6. |
| 43 | Ch. 5: Calibration prose | `($0.801$ against $0.819$, $0.896$ against $0.937$)` | `($0.801$ against $0.821$, $0.896$ against $0.936$)` | Match the table. |
| 44 | Ch. 5: Shrinkage pair | `($\kappa_j = 0.17$ and $0.19$)` ... `$0.906$ against $0.898$` | `($\kappa_j = 0.17$ and $0.18$)` ... `$0.907$ against $0.904$` | Entertainment $0.172/0.907$, Business $0.183/0.904$. Still tied, still in that order. |
| 45 | Ch. 4: all five table bodies | `tab:prior-range` 10 rows, `tab:hs-range` 8, `tab:hs-kappa` 22x3, `tab:hs-signal` 22x3, `tab:structzero` 22x4 -- all from the pre-rename `.rds` | The same five tables regenerated from the post-19:32 `.rds` | `04b` and `04d` had not run since 3 September; the batch loop only listed `04a` and `04c`, so they never ran against the renamed `class` column. 84 rows in total, generated by script from the `.rds` rather than retyped. Row order changed in `tab:hs-kappa` and `tab:structzero` because the sort moved. |
| 46 | Ch. 4: one predictor flips, not two | `Across a hundred-fold range of the global scale, one of the $22$ predictors changes its signal/noise classification.` | `Across a hundred-fold range of the global scale, two of the $22$ predictors change their signal/noise classification.` | `Cleanliness` moved from $\kappa = 0.509$ to $0.498$ at $s = 0.1$, crossing the $0.5$ threshold. It now reads signal at the small scale and noise at the other two, so it joins `Online support` as unstable. |
| 47 | Ch. 4: largest shift doubled | `the largest shift in any posterior mean is $0.018$ on the log-odds scale` | `the largest shift in any posterior mean is $0.036$ on the log-odds scale` | `Personal Travel` moves $0.036$ across the three scales. The argument is unaffected -- $0.036$ on the log-odds scale is still negligible -- but the number has to be right. |
| 48 | Ch. 4: four effects gain significance, not three | `Three weak effects (\texttt{Flight Distance}, \texttt{Age}, \texttt{Mid-flight delay}) acquire intervals excluding zero` | `Four weak effects (\texttt{Flight Distance}, \texttt{Age}, \texttt{Personal Travel}, \texttt{Mid-flight delay}) acquire intervals excluding zero` | `Personal Travel` goes from $[-0.546, 0.189]$ at $n = 1213$ to $[-0.321, -0.118]$ at $n = 15003$. |
| 49 | Ch. 4: contraction factor now exceeds the prediction | `contracts the intervals by a median factor of $3.42$, against the $3.52$ that $\sqrt{n}$ asymptotics predicts` | `contracts the intervals by a median factor of $3.54$, in line with the $3.52$ that $\sqrt{n}$ asymptotics predicts` | The median is now $3.535$ against $\sqrt{15003/1213} = 3.517$. The old sentence was built on the observed factor falling *short* of the prediction; it now sits marginally above it, so "against" has to become "in line with". |
| 50 | Ch. 4 prose: disloyal Customer barely moves | `\texttt{disloyal Customer} by $0.014$` | `\texttt{disloyal Customer} by $0.004$` | $-1.845$ at $n=1213$ against $-1.841$ at $n=15003$. |
| 51 | Ch. 4 prose: Seat comfort shift | `\texttt{Seat comfort} by $0.022$` | `\texttt{Seat comfort} by $0.023$` | $0.632$ against $0.610$. |
| 52 | Ch. 4: structural-zero comparison, Dep/Arr | `it gets \emph{more} negative (from $-0.335$ to $-0.513$)` | `(from $-0.334$ to $-0.506$)` | Ch. 3 value and 04c value both moved. The finding is unchanged: it still gets more negative. |
| 53 | Ch. 4: structural-zero comparison, Seat comfort | `$\kappa$ drops from $0.367$ to $0.209$, $\beta$ rises from $+0.419$ to $+0.881$` | `$\kappa$ drops from $0.370$ to $0.215$, $\beta$ rises from $+0.424$ to $+0.869$` | Same. |
| 54 | Ch. 4: structural-zero comparison, Food and drink | `($\kappa = 0.492$, $\beta = -0.275$) to firmly shrunk ($\kappa = 0.733$, $\beta = +0.020$)` | `($\kappa = 0.486$, $\beta = -0.280$) to firmly shrunk ($\kappa = 0.732$, $\beta = +0.025$)` | Same. |
| 55 | Ch. 2 MCMC settings: iterations | `Iterations (kept) & 6000` | `Iterations & 12000` and `Retained draws & 5142` | The notebook runs `iter = 12000` with `thinning = 7` over 3 chains, giving $12000/7 \times 3 = 5142$ retained draws -- the exact row count of `beta_draws`. "6000" matched neither the old run (1284 draws) nor the new one. Splitting the row into iterations and retained draws is clearer than one ambiguous "kept". |
| 56 | Ch. 3 MCMC settings: adaptation | `Adaptation & 1500` | `Adaptation & 5000` | `03_feature_selection.Rmd:96` sets `n_adapt = 5000`. This was wrong before the re-runs too. |
| 57 | Ch. 3 MCMC settings: iterations | `Iterations (kept) & 5000` | `Iterations & 30000` and `Retained draws & 30000` | `iter = 30000`, `thinning = 3`, 3 chains: $30000/3 \times 3 = 30000$ draws, matching `beta_draws`. |
| 58 | Ch. 2 prose: effective sample sizes | `effective sample sizes range from $1123.153$ to up to $2799.333$` | `effective sample sizes range from $2057$ to $5370$` | Measured with `coda::effectiveSize` on the current draws. Also drop the three decimals -- an ESS to a thousandth is spurious precision, and "range from ... to up to" is not grammatical. |
| 59 | Appendix B: ESS table | All ten values in `tab:ess` (`appendix/logit_convergence.tex:66-76`) | `$\beta_1$ 5142, $\beta_2$ 5370, $\beta_3$ 5142, $\beta_4$ 4147, $\beta_5$ 2456, $\beta_6$ 5142, $\beta_7$ 2447, $\beta_8$ 5169, $\beta_0$ 2057, $\beta_{\text{mid}}$ 4476` | Every one is stale. Note the `S[table-format=4.3]` column spec expects three decimals -- change it to `table-format=4.0` when the decimals go. |
| 60 | Chapter 3 pointed at an appendix that does not exist | `The complete diagnostic material -- all trace and autocorrelation plots for both the $\beta$ and the $\lambda$ parameters, together with the full Gelman--Rubin and effective-sample-size tables -- is collected in Appendix \ref{app:mcmc_horseshoe}.` | `The remaining diagnostics -- trace and autocorrelation plots for the other $\beta$ and $\lambda$ parameters, with the full Gelman--Rubin and effective-sample-size tables -- were inspected in the accompanying notebook (\texttt{03\_feature\_selection.Rmd}) and are not reproduced here.` | `\label{app:mcmc_horseshoe}` sat on the BAS *Parameter-space convergence* section, which contains none of that material. `appendix/horsehose_convergence.tex` -- the file that presumably should have held it -- is **empty (0 bytes)**, is not `\include`d in `main.tex`, and its filename is misspelled. So the chapter promised a complete diagnostic appendix and sent the reader to the wrong section, which happens to be about a different model. Rewritten to claim only what the document delivers. The unused `\label{app:mcmc_logit}` was removed at the same time. |
| 61 | Double slash in an image path | `\includegraphics[width=0.5\linewidth]{img//data_expl/boxplot_skew.png}` | `\includegraphics[width=0.5\linewidth]{img/data_expl/boxplot_skew.png}` | Found while enumerating figure paths. `graphicx` tolerates it, but it breaks any tooling that resolves paths literally -- including the check above, which had to normalise it. |
| 62 | Empty appendix, weakened claim | `appendix/horsehose_convergence.tex` -- 0 bytes, not `\include`d, filename misspelled. Chapter 3: `The remaining diagnostics ... were inspected in the accompanying notebook (\texttt{03\_feature\_selection.Rmd}) and are not reproduced here.` | `appendix/horseshoe_convergence.tex` -- 277 lines, `\include`d from `main.tex`, `\label{app:horseshoe_convergence}`. Chapter 3: `The complete diagnostic material -- all trace and autocorrelation plots for both blocks, together with the full Gelman--Rubin and effective-sample-size tables -- is collected in Appendix~\ref{app:horseshoe_convergence}, where the three imperfectly converged scales are identified and their consequences discussed.` | The empty file was deleted and replaced by a correctly spelled one. The report now delivers what the chapter claims. |
| 63 | $\tau$ called the worst-mixing parameter | `the scale parameters, and $\tau$ in particular, mix more slowly and show higher autocorrelation, as expected for a horseshoe, but reach effective sample sizes adequate for the summaries we report` | `The local scales $\lambda_j$ mix more slowly and show higher autocorrelation, as expected for a horseshoe, and three of them sit at or above the conventional $1.1$ threshold; the global scale $\tau$, by contrast, is among the better-behaved parameters in that block.` | The opposite of the truth for $\tau$. It has PSRF $1.00$ and ESS \num{4515} -- the second-highest in its block, behind only $\lambda_4$ at \num{4882}. The local scales run down to \num{1388}. The block as a whole does mix more slowly than the coefficients, so that half of the sentence was right; singling out $\tau$ was not. |
| 64 | "Three limitations" followed by two | `Three limitations are worth naming. The structural zeros were flagged and tested but not modelled with a zero-inflated likelihood; $n \approx 1200$ is modest for twenty-two predictors, which is why several effects are borderline.` | Same opening, then the two existing limitations, then: `and three of the horseshoe's local scales did not reach the conventional convergence threshold (Appendix~\ref{app:horseshoe_convergence}). Tripling the chain length left them unchanged, so this is weak identification rather than insufficient sampling: for a covariate the data barely constrains, the amount of shrinkage applied to it is itself barely constrained. The coefficients those scales act on did converge, and the sensitivity analysis of Section~\ref{sec:sens-horseshoe} shows the signal/noise verdicts survive a hundred-fold change in the global scale, so the conclusions above stand; but the precise shrinkage weight of three weak covariates should not be read to three decimals.` | The sentence promised three limitations and named two. The convergence caveat is the natural third, so one edit closes both. |
| 65 | Appendix section added twice | `appendix/horseshoe_convergence.tex`, 481 lines, `\section{Horseshoe shrinkage weights}` at both `:307` and `:398` | 396 lines, one copy | Four multiply-defined labels: `app:hs_shrinkage`, `fig:hs_shrinkage`, `tab:hs-kappa`, `tab:hs-signal`. `HEAD` had 304 lines and no such section, so both copies were uncommitted: a rewrite appended instead of replacing. The kept copy takes the fuller intro from the first and the caption and `width=0.62\linewidth` from the second. 32 pages to 30. |
| 66 | Figure 1 and Table 1 alone on a page | `\include{chapters/00_prob_desc_data_expl}`; `\begin{figure}` with no placement specifier | `\input{...}`; `\begin{figure}[htbp]`; `\topfraction` 0.9, `\bottomfraction` 0.8, `\textfraction` 0.07, `\floatpagefraction` 0.8 | Two causes. Ch. 1 was the only chapter pulled in with `\include`, whose implicit `\clearpage` flushes deferred floats, and it ends at its `\end{table}` with no trailing text. That alone did not fix it: LaTeX still built a dedicated float page, because the pair exceeds `\floatpagefraction = 0.5` of the 25.30 cm text block. 30 pages to 29. |
| 67 | Table bodies set at body-text size | 13 of the 20 `table` environments declared no size | `\small` as the first line inside each | The other seven already declared one and keep it, including the `\footnotesize` ones, which `\small` would have made *larger*. Note that `\AtBeginEnvironment{table}{\small}` is inert for floats: `latex.ltx:15855` defines `\@floatboxreset` as `\reset@font\normalsize\@setminipage` and runs it inside the float box, discarding any size set before it. Verified by rendering all 29 pages to images: with the hook, 0 pages changed even at `\tiny`; with `\small` inside each table, 16 changed. Comparing extracted text proves nothing, as `pdftotext` is size-blind. |
| 68 | Figure 2 subfigure sizes | both minipages `0.49\linewidth` | left `0.343`, right `0.637` | Requested, not a defect. Trace plots (a) and (b) 30% smaller, marginal posteriors (c) 30% larger; the two sum to 0.98 so the row still fits. Scaling the images instead would have pushed (c) past the right margin. At `0.343` the axis labels inside the trace plots are close to illegible in print; `0.40 / 0.58` would keep them readable. |
| 69 | `tab:hs-signal` folded into `tab:hs-kappa` | Two 22-row tables: shrinkage weights, then the same weights as yes/no verdicts | One 22-row table with a `stable` column and a mid-rule at $\kappa_j = 0.5$ | The verdict table was `tab:hs-kappa` with a threshold applied and carried no information of its own; this audit had already named it the largest cut available. Structure adopted from Guflie's Overleaf revision, but the body was regenerated from `data/04b_horseshoe_sweep.rds` rather than copied: his numbers predate the 04b re-run. 11 of 22 predictors are signal at $s=1$; `Online support` and `Cleanliness` are the two unstable ones, consistent with issue 46. |
| 70 | **OPEN** -- the Overleaf revision carries pre-re-run numbers | Guflie's `tab:hs-kappa-signal`: `disloyal Customer` 0.052/0.071/0.076, `Class: Business` 0.145/0.180/0.191, `On-board service` 0.312/0.364/0.378 | The repository values, which match `04b_horseshoe_sweep.rds` exactly: 0.050/0.070/0.076, 0.134/0.184/0.192, 0.302/0.367/0.375 | Only 3 of the 66 values agree; 63 differ. His `Cleanliness` reads 0.509 at $s=0.1$, the value issue 46 records as superseded, which is what dates the copy. His revision also references `\ref{tab:hs-signal}`, which no longer exists in it, and `\ref{tab:hs-kappa}`, which resolves to a leftover copy in `bas_comparison.tex`. Six of his figures (`sensitivity/*`, `ppc_subgroups`, `bas_crosscheck/*`) are the pre-regeneration versions. Do not merge that revision wholesale. |
| 71 | **OPEN** -- hardcoded section numbers now point at the wrong chapters | `Section~5` for out-of-sample validation (`00_prob_desc_data_expl.tex:16`); `Section~4.1`--`4.5` (`04_sensitivity.tex:10-15`); `Section~02` (`:32`); `Section~03` (`:96,98,105`) | `\ref{}` to the relevant labels | The numbering is 1 Problem, 2 Model spec, 3 Logit, 4 Horseshoe, 5 Sensitivity, 6 Prediction, 7 Conclusions, so validation is Section 6 and the sensitivity subsections are 5.1-5.5. Same class as issue 16, which fixed the hardcoded appendix letter. They are coloured hyperlinks, so a reader clicking one lands in the wrong chapter. |
| 72 | Floats stacked above their prose instead of beside it | Ch. 1: correlation figure, then prose, then the class-balance table, each full width. Ch. 2: MCMC settings table, then the convergence paragraph below it. Ch. 4: `prior_sweep` figure, then prose, then the prior-range table | One float per pair, two `minipage`s side by side: 0.64/0.33, 0.34/0.62 and 0.55/0.42 | Layout taken from Guflie's Overleaf revision, which is where the page saving in it comes from; the numbers are the repository's throughout, since his prior-range table still reads `Intercept` 0.022 against the measured 0.010 and his $\kappa$ table predates the 04b re-run (issue 70). Body 13 printed pages to 12, document 29 to 28. |
| 73 | `\captionof{table}` inside a figure float breaks the figure numbering | Ch. 1 float captioned `Figure 2` while its own subfigures read `1a`/`1b`; no `Figure 1` existed in the document | `\addtocounter{figure}{-1}` after the `\captionof{table}` | Only where the float also carries its own `\caption`. In the chapter 4 float, which captions both minipages with `\captionof`, the same correction produced two figures numbered 4, so it is not applied there. Guflie's revision carries the chapter 1 form of this bug. Verified from `main.aux`: figures now run 1-8 and tables 1-6 with no gap or repeat. |
| 74 | Body still four pages over target | 12 printed pages; `horseshoe_sweep` and its range table stacked; `beta_forest` at `0.85\linewidth` alone on a 361-word page; `structural_zero` at `0.9` | `horseshoe_sweep` and the range table side by side (0.46/0.50); `beta_forest` moved to Appendix D at `0.62`; `structural_zero` narrowed to `0.62`; three redundant trailing clauses cut from Ch. 3 | Body 12 to 11 printed pages, document 28 to 27, matching the Overleaf revision's page count. `beta_forest` is moved rather than deleted: `TODO.md` lists it under "do not touch" as a headline horseshoe result, and Guflie's revision drops it outright. The prose it is referenced from now points at the appendix. |
| 75 | **NOT TAKEN** -- three defects in the Overleaf prose | `\cite{waic_ref}` and `\cite{gr_ref}`; "weakly indpendent informative Gaussian prior"; the JAGS sampling statement $\texttt{midflight\_delay}_i \sim \mathcal{N}(0,1)$ dropped from Ch. 2 | `\cite{bl_book}`; our wording; the statement kept | The two citation keys exist in neither `biblio.bib`, because they are the entries issue 19 removed at the author's instruction; importing that prose would reintroduce two undefined citations. Guflie's chapter 1 prose is otherwise identical to ours apart from an en-dash where we have an em-dash, so the page difference between the two revisions was layout, not text: his body carries 6798 words against our 7043, a 3.5% difference. |
| 76 | Body at the 8-10 page target, by layout alone | 11 printed pages; the structural-zero table and its figure stacked; the horseshoe MCMC settings table alone above its convergence paragraph; the posterior-predictive figure and the loss table stacked | Three more pairs set as two `minipage`s: structural-zero table (0.57, `\footnotesize`) with its figure (0.40); horseshoe MCMC settings (0.30) with the convergence paragraph (0.66); posterior-predictive figure (0.56) with the loss table (0.40) | Body 11 printed pages to 10, document 27 to 26. All 22 rows of the structural-zero table and all three rows of the loss table are kept; nothing is cut. A global `\arraystretch`/`\tabcolsep` tightening was tried first and reverted: it moved no page boundary and would have reached into the appendices, which are not over budget. |
| 77 | Calibration table stacked above its own paragraph | `tab:calibration` full width, the paragraph discussing it below | Table (0.44) and paragraph (0.52) as two `minipage`s | Taken from the 6 September 18:27 Overleaf export, the one piece of layout in it we had not already arrived at independently. All ten deciles are kept. In chapter 5 that export reaches the same three pairings as this repository by a different route, so nothing else there was portable. |
| 78 | **OPEN** -- two live duplicate labels in the Overleaf export | `tab:mcmc_settings` in `02_logit_model.tex` **and** `bas_comparison.tex`; `tab:hs_mcmc_settings` in `03_horseshoe_model.tex` **and** `bas_comparison.tex` | Delete the appendix copies | Issues 9 and 10 again, by the route issue 13 describes: the material was moved to the appendix during page-count cutting and the chapters were later restored without the appendix copies being removed. The Romanian marker comment issue 23 deleted, `% ---- mutate din corp pentru a respecta limita de pagini ----`, is back at `bas_comparison.tex:77` and still heads the block. That file also carries `\label{app:mcmc_logit}\label{app:mcmc_horseshoe}` on one line, leaving the second unreachable. Six further duplicate labels in that export sit inside `\begin{comment}` blocks and are inert, but they are dead source in a deliverable. This repository has none. |
| 79 | Pair proportions aligned to the Overleaf revision | Horseshoe sweep 0.46/0.50 with `\hfill`; structural-zero 0.57/0.40 with `\hfill`, figure at `\linewidth`; range table headed "Range of posterior mean" | Horseshoe sweep 0.4/0.36 with `\hspace{4cm}`; structural-zero 0.50/0.46 with `\hspace{.5cm}`, figure at `0.7\linewidth`; header shortened to "Range" | Adopted for consistency with the Overleaf revision, at a measured cost: the document goes 26 printed pages to 27, the extra page falling in the appendices, and one overfull box of 7.86pt appears in the covariate list at `04_sensitivity.tex:159-168`. The body stays at 10 either way, so the page target is unaffected. The header had to be shortened as well: at 0.36 the longer one overflowed its minipage by 19.74pt, which is why that revision carries the short form. The previous proportions are one revert away if the appendix page matters more than matching. |
